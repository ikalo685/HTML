
  function internet() {
            var x = document.getElementById("search").value;

            if (x === "dog") {
                window.open("/index.html");
            }

            if (x === "cat") {
                window.open("/customizedalert.html");
            }

        }